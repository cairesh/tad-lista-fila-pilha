#include "fila.h"
/* TODO: inicializar fila circular e controlar inicio/fim.
   enqueue insere no final; dequeue remove do inicio (FIFO).
   Tratar overflow e underflow; implementar vazia/cheia e exibicao.
   Retornar a struct modificada por valor. Se necessario, usar registro
   auxiliar para retornar tambem valor removido e estado. */
/* Declaracao provisoria para manter o modulo C valido. */
typedef int FilaImplementacaoPendente;
