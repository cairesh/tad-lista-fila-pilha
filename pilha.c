#include "pilha.h"
/* TODO: inicializar topo; implementar verificacoes de vazia/cheia.
   push empilha; pop retira do topo (LIFO).
   Tratar overflow e underflow; implementar exibicao.
   Retornar a struct modificada por valor. Se necessario, usar registro
   auxiliar para retornar tambem valor removido e estado. */
/* Declaracao provisoria para manter o modulo C valido. */
typedef int PilhaImplementacaoPendente;
